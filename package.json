{
  "name": "teduh-unit-counter",
  "version": "1.0.0",
  "description": "unit tests for popup.js using jest and jsdom",
  "scripts": {
    "test": "jest",
    "pack": "zip -r teduh.zip manifest.json popup.html popup.css popup.js house-icon.png",
    "clean:pack": "rm -f *.zip && npm run pack"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jsdom": "^23.0.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
